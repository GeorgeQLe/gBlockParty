# gBlockParty — Implementation History

## 2026-04-25
- Phase 2 / Step 2.4: Wired home page route (`apps/web/src/app/page.tsx`) to load real content. Server component calls `loadAllGBlocks({ contentRoot: path.join(process.cwd(), "content") })` and renders `FeaturedRail` → `ShortsRail` → `FirehoseFeed` in order. `searchParams` (Next.js 15 async `Promise`) wired to `FirehoseFeed`'s `activeType`/`activeCollection` props for URL-param-based filtering (`?type=tutorial`, `?collection=gcanbuild`). Graceful empty state when no gBlocks exist (friendly message instead of blank sections). Added `metadata` export for SEO (`title` + `description`). Page is now dynamic (`ƒ`) due to `searchParams` usage. `pnpm -w test` 19/19 green, `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (home page renders with fixture data).
- Phase 2 / Step 2.3: Created 3 home-page section components under `apps/web/src/components/`: `FeaturedRail.tsx` (filters `featured: true`, sorts by `publishedAt` desc, limits to 3, renders `GBlockCard`s in responsive grid, returns `null` when empty), `FirehoseFeed.tsx` (renders all blocks reverse-chron with `GBlockCard`s, link-based type and collection filter chips using `<a>` tags for server-component compatibility, active chip styled with inverted colors, graceful empty state), `ShortsRail.tsx` (filters to `type === "clip"`, horizontal scroll with `overflow-x-auto`, fixed-width cards, returns `null` when no clips). All server components, no `"use client"`. Props accept `GBlock[]` from `@gblockparty/gblock-schema`. `FirehoseFeed` also accepts optional `activeType`/`activeCollection` for URL-param-based filtering. `pnpm -w test` 19/19 green, `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (4 static pages).
- Phase 2 / Step 2.2: Created 3 shared UI primitives under `apps/web/src/components/`: `TypeBadge.tsx` (pill badge colored by type category — accent-coral for video types, accent-blue for code types, ink for text types), `CollectionBadge.tsx` (linked pill badge with neutral surface styling, navigates to `/<collection>`), `GBlockCard.tsx` (composes both badges with brutal border + hard-offset shadow, hero image or type-colored placeholder, linked title to `/<collection>/<slug>`, formatted date, CSS-only hover lift effect using translate + shadow-brutal-lg). All server components, no `"use client"`. Types imported from `@gblockparty/gblock-schema`. Tailwind utilities resolve against Step 1.5 design tokens. `pnpm -w test` 19/19 green, `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (4 static pages).
- Phase 2 / Step 2.1: Seeded 5 development fixture gBlocks under `content/gblocks/` across all 3 collections. Files: `gcanbuild/pastebin-clone-nextjs.mdx` (tutorial, featured), `gcanbuild/better-auth-tutorial.mdx` (tutorial), `weekly-sota/sota-ep-001.mdx` (episode), `weekly-g/weekly-g-ep-001.mdx` (episode, featured), `gcanbuild/streaming-highlight-clip.mdx` (clip, parentSlug refs pastebin tutorial). Coverage: 3 types (tutorial, episode, clip), all 3 collections, 2 featured, 1 clip for shorts rail. Each MDX body uses at least one component stub (`<YouTube>`, `<Callout>`, `<RepoCard>`). `loadAllGBlocks({ contentRoot: "content" })` returns 5 blocks, all validate against `gBlockSchema`. `pnpm -w test` 19/19 green (no regression), `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (4 static pages).
- Phase 2 planning: Decomposed Phase 2 (UI Surface) into 11 implementation steps using `tests-after` strategy. 3 parallel lanes: components (GBlockCard, TypeBadge, CollectionBadge, FeaturedRail, FirehoseFeed, ShortsRail), routes (home, collection, gBlock detail, redirect, tag stub), paywall (PaywallCard). Step 2.1 seeds 5 development fixture gBlocks across all 3 collections exercising 3 types (tutorial, episode, clip) with `featured` and shorts-rail coverage. Steps 2.2–2.3 build presentational components as a micro-serial preamble. Steps 2.4–2.7 build routes in parallel with Step 2.8 (PaywallCard). Step 2.9 handles build-time slug uniqueness verification + SEO metadata polish. Steps 2.10–2.11 are the Green steps (regression tests + full verification). No manual tasks. Updated `tasks/roadmap.md` (inserted execution profile + implementation steps into Phase 2 section) and `tasks/todo.md` (replaced Phase 1 completed queue with Phase 2 working document).

## 2026-04-24
- Phase 1 / Step 1.1: Vitest scaffolded as the monorepo test runner (`vitest.config.ts`, root + `@gblockparty/gblock-schema` scripts, `test` task wired into `turbo.json`). `pnpm -w test` exits 0 with no tests present.
- Phase 1 / Step 1.2: Added 14 failing Vitest cases at `packages/gblock-schema/src/__tests__/schema.test.ts` pinning the discriminated-union contract (per-type required fields, episode videoUrl/audioUrl OR, unknown type, shared optional fields, membership default). Also added a package-local `packages/gblock-schema/vitest.config.ts` so `pnpm --filter` picks up `src/**/*.test.ts` (root include pattern didn't match when CWD is the package). Tests import via relative path (`../index`) so failures trace to the current flat schema, not module resolution. `pnpm --filter @gblockparty/gblock-schema test` and `pnpm -w test` both exit non-zero (7 red / 7 green) — the intended red state for Step 1.4 to target.
- Phase 1 / Step 1.3: Added failing MDX loader tests at `apps/web/src/lib/content/__tests__/loader.test.ts` pinning the contract for `loadAllGBlocks({ contentRoot })` and `loadCollection(slug, { collectionsRoot })` (DI via options, no env coupling). Five cases: valid fixtures parse + include body, invalid-frontmatter error surfaces the offending file path (`broken-episode.mdx`), duplicate `shared-slug` across collections throws, valid `gcanbuild.yaml` parses, invalid YAML throws. Fixtures seeded under `__tests__/fixtures/{valid,invalid,dupe}/gblocks/<collection>/<slug>.mdx` and `__tests__/fixtures/collections/{gcanbuild,bad}.yaml` — each content root has its own `gblocks/` tree so Step 1.6's loader just walks `<contentRoot>/gblocks/**`. Added `apps/web/vitest.config.ts` (package-local `include`) and `test` / `test:watch` scripts in `apps/web/package.json`. `pnpm --filter @gblockparty/web test` fails on missing `../loader` module (the red signal); `pnpm -w test` fails with the 7 schema reds plus the loader suite unable to resolve imports. Step 1.2 schema reds still present and unchanged.
- Phase 1 / Step 1.5: Replaced the placeholder TODO in `apps/web/src/app/globals.css` with the playful-brutalist token set per spec §4, exposed via a Tailwind v4 `@theme { ... }` block so `bg-bg`, `text-ink`, `shadow-brutal`, `rounded-md`, `font-mono`, etc. resolve as utilities. Tokens: colors (`--color-bg` cream `#FFF8E7`, `--color-ink` `#0B0B0B`, `--color-ink-soft` `#2A2A2A`, `--color-paper`, `--color-muted`/`--color-surface` `#EDE4C9`, `--color-primary`/`--color-accent-lime` `#C3F53C`, `--color-accent`/`--color-accent-coral` `#FF6B5B`, `--color-accent-blue` `#4D7CFE`); border widths 2.5px/3px + `--border-ink`; hard-offset no-blur shadows (`--shadow-brutal` 4px 4px, `--shadow-brutal-sm` 2px, `--shadow-brutal-lg` 6px); boxy radii (0 / 6 / 12 / 20); Inter Variable (sans/display) + JetBrains Mono Variable (mono) font stacks with system fallbacks — no `@font-face` / `next/font` this step (fonts slide to Step 1.10 per plan); px scale 12–72; motion with `--ease-out`/`--ease-in` per spec plus a `--ease-brutal: steps(4, end)` option. Body defaults applied on `html`/`body`; `code`/`kbd`/`pre`/`samp` get the mono stack. `postcss.config.js` untouched — Tailwind v4's inline `@theme` worked against the existing `@tailwindcss/postcss` plugin. `pnpm --filter @gblockparty/web build` succeeds (4 static pages); `pnpm --filter @gblockparty/gblock-schema test` exits 0 (14/14, no regression); `pnpm -w test` still red only on the Step 1.3 loader suite (unchanged shape, Step 1.6 scope).
- Phase 1 / Step 1.6: Installed MDX tooling (`gray-matter`, `js-yaml`, `next-mdx-remote` in deps; `@types/js-yaml` in devDeps) and implemented the content loader at `apps/web/src/lib/content/{paths,loader,collections,index}.ts` — sync API matching the Step 1.3 test contract. `loadAllGBlocks({ contentRoot })` walks `<contentRoot>/gblocks/<collection>/<slug>.mdx`, `gray-matter`-parses each file, validates frontmatter with `gBlockSchema.safeParse(data)` (body kept separate — schema doesn't include a `body` field, so it's attached post-parse), throws `${relativePath}: ${zodMessage}` on validation failure (Step 1.3 asserts `/broken-episode\.mdx/`), and maintains a `Map<slug, relativePath>` to throw on cross-collection duplicates with the slug named in the message. `loadCollection(slug, { collectionsRoot })` reads `${slug}.yaml`, `js-yaml`-parses, and lets `collectionSchema.parse()` throw. MDX component stubs seeded at `apps/web/src/components/mdx/{YouTube,RepoCard,Callout,AudioPlayer}.tsx` — minimal props-only placeholders (Phase 1 milestone; not exercised by Step 1.3 tests). No `import "server-only"` — Vitest runs in Node and the plain `fs` usage is fine; adding the guard would have tripped the test env. `pnpm --filter @gblockparty/web test` exits 0 (5/5); `pnpm -w test` exits 0 for the first time in Phase 1 (19/19 total); `pnpm -w -r typecheck` clean; `pnpm --filter @gblockparty/web build` still green (4 static pages). No changes outside the file list.
- Phase 1 / Step 1.7: Seeded the three active-collection YAMLs under `content/collections/` per spec §2. Overwrote the existing `gcanbuild.yaml` (which carried a paraphrased description) with verbatim spec copy, and created `weekly-sota.yaml` + `weekly-g.yaml`. Each file carries `slug` + `name` + `description` (verbatim §2); `frontDoorUrl` omitted on all three (spec §2 doesn't name a destination — the field is optional). Descriptions are long-form and include spec-level context (e.g. the Mailchimp-Clone underperformance note on GCanBuild) because the plan called for verbatim copy. Verified via a one-off `node --input-type=module` probe from `apps/web/` that `js-yaml.load` + `collectionSchema.parse()` accepts all three (no throwaway test committed). Pre-existing `content/collections/boston-founder-radio.yaml` left alone — it's a scheduled Phase 4 decommission (spec §2 marks BFR dropped, and it's listed in todo.md's Follow-ups) but out of scope for Step 1.7. `pnpm -w test` 19/19 green; `pnpm -w -r typecheck` clean (turbo-cached); `pnpm --filter @gblockparty/web build` still succeeds (4 static pages). No git remote — push is a local no-op.
- Phase 1 / Step 1.8: Green-bar verification — cold-ran `pnpm turbo run test typecheck --force` (cache bypass, no incremental mask): `@gblockparty/gblock-schema:test` 14/14, `@gblockparty/web:test` 5/5 (19/19 workspace-wide), typecheck clean across both packages. Cold rebuild (`rm -rf apps/web/.next && pnpm --filter @gblockparty/web build`) green — 4 static pages, compiled in ~1.5s. Probed `loadCollection()` against the real `content/collections/` tree via an ephemeral vitest file under `apps/web/src/tmp-probe/probe.test.ts` (stdout-only, deleted after run — not committed): all three slugs parsed and returned `GCanBuild` / `Weekly SOTA` / `Weekly G`. Same probe exercised `loadAllGBlocks({ contentRoot: '/Users/georgele/projects/apps/gblockparty/content' })` → returned `[]` as expected against the empty `content/gblocks/` (loader's `fs.existsSync` short-circuit worked). Aside: tried `node --experimental-strip-types` first — hit `ERR_MODULE_NOT_FOUND` on `./paths` because the loader files use extensionless imports (TS bundler resolution), which node's native strip-types resolver doesn't handle; vitest's esbuild resolver handles it, so routed the probe through it. No production diff surfaced — loader + collections modules are clean. Diff confined to `tasks/{todo,history}.md`. Push is still a local no-op (no remote on `master`).
- Phase 1 / Step 1.9: Workspace typecheck + build formal gate — cold-ran `pnpm -w -r typecheck --force` (2 packages, turbo cache bypassed): `@gblockparty/gblock-schema:typecheck` + `@gblockparty/web:typecheck` both clean in ~880ms. Cold build (`turbo cache cleared + pnpm --filter @gblockparty/web build`): 4/4 static pages, compiled in ~680ms. Both gates exit 0. No production diff — this step exists as a dedicated sign-off boundary before the Step 1.10 refactor. Diff confined to `tasks/{todo,history}.md`.
- Phase 1 / Step 1.10: Final refactor pass — converted `baseFields` plain-object spread in `packages/gblock-schema/src/index.ts` to a proper `baseSchema = z.object({...})` with `.extend()` per variant, which is more idiomatic Zod and eliminates the raw field-object intermediary. Cleaned barrel exports at `apps/web/src/lib/content/index.ts` to re-export all public symbols: added `LoadAllGBlocksOptions` and `LoadCollectionOptions` type exports that were missing. No other cleanup surfaced — loader, collections, and paths modules were already clean. `pnpm -w test` 19/19 green; `pnpm -w -r typecheck` clean (2 packages); `pnpm --filter @gblockparty/web build` green (4 static pages). Phase 1 milestone fully checked off.
- Phase 1 / Step 1.4: Rewrote `packages/gblock-schema/src/index.ts` as `z.discriminatedUnion("type", […])` over 8 variant schemas built from a shared `baseFields` spread. `episode` video-or-audio requirement is enforced via union-level `.superRefine()` (not `.refine()` on the variant) because Zod 3.25's `discriminatedUnion` refuses `ZodEffects` members — the earlier plan's assumption that `.refine()` on a variant worked turned out to be false in this Zod line; `.superRefine()` at the union level preserves per-variant strictness and keeps the union shape intact. `gBlockTypeSchema` expanded to the full 8-type enum (`clip`, `stream` added). Shared optional surface fields (`heroImage`, `videoUrl`, `featured`, `seriesSlug`) now live on every variant via the base spread. Also fixed a pre-existing Step 1.2 typecheck bug in `packages/gblock-schema/src/__tests__/schema.test.ts` — factory helpers typed their `overrides` as `Record<string, unknown>`, which made destructures like `const { videoUrl, ...rest } = stream()` fail TS2339; switched to generic `<T extends Record<string, unknown>>` so spread preserves keys in the return type. `pnpm --filter @gblockparty/gblock-schema test` exits 0 (14/14); `pnpm --filter @gblockparty/gblock-schema typecheck` clean. `pnpm -w test` still non-zero — only the Step 1.3 loader suite is red (unresolved `../loader` / `../collections`); schema regressions: none. `pnpm -w -r typecheck` — schema package clean; `apps/web` still red on the same loader imports (carried over from Step 1.3, Step 1.6 scope).
