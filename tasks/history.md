# gBlockParty — Implementation History

## 2026-04-27
- **Phase 4 complete.** Step 4.9: Final verification gate — ran `pnpm -w test` (42/42 green: 14 schema + 5 loader + 23 pages), `pnpm -w -r typecheck` (clean, 2 packages), `pnpm --filter @gblockparty/web build` (11 pages generated). All three commands exit 0 with no regressions. Phase 4 milestone fully checked off: all 7 acceptance criteria met. Phase 4 shipped 9 steps: Plausible analytics embed, YouTube view-count scraper + GitHub Action, view counts on detail pages, cross-repo BFR decommission + gBlockParty portfolio entry in lexcorp-war-room, PaywallCard visual polish, 9 regression tests, and final verification. All 4 phases now complete — project is at v1 feature-complete.
- Phase 4 / Step 4.8: Added 9 Phase 4 regression tests to `apps/web/src/__tests__/pages.test.ts` in three new `describe` blocks. `loadViewCounts`: (1) returns empty Map when `youtube-views.json` doesn't exist, (2) parses valid JSON fixture and returns correct videoId→count Map (uses temp dir with `data/youtube-views.json`). `extractVideoId`: (3) `/watch?v=`, (4) `/shorts/`, (5) `/embed/`, (6) `youtu.be/` URL patterns all extract correct ID, (7) invalid URLs return null. Scraper script: (8) `scripts/scrape-youtube.mjs` exists on disk, (9) passes `node --check` syntax validation. Skipped duplicate `extractPreview` tests — already covered by Phase 2 regression suite (3 cases). `pnpm -w test` 42/42 green (14 schema + 5 loader + 23 pages), `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (11 pages).
- Phase 4 / Step 4.7: PaywallCard visual + copy polish. Updated `apps/web/src/components/PaywallCard.tsx`: heading "Become a member to read this gBlock" → "Unlock the full gBlock", button "Join gBlockParty" → "Get member access", added subtitle "Full tutorials, source code, and exclusive episodes.", added `opacity-60 cursor-not-allowed` to disabled button, tightened spacing (`px-6 pt-6 pb-24` → `px-5 pt-5 pb-20`, gradient area `px-6 pb-6 pt-16` → `px-5 pb-5 pt-14`), smoothed gradient (`from-60%` → `from-50%`). Visual verification with temporary `membership: member` fixture confirmed card renders correctly. No functional changes — `extractPreview` and paywall logic untouched. `pnpm -w test` 33/33 green, typecheck clean, build green.
- Phase 4 / Step 4.6: Registered gBlockParty in lexcorp-war-room portfolio. Created `/Users/georgele/projects/apps/lexcorp-war-room/portfolio/gblockparty.yaml` with `slug: gblockparty`, `name: gBlockParty`, `businessUnit: LexContent`, `status: Building`, `lifecycleState: active`, `freshness: active`. Includes all 6 spec §9 KPIs in `metrics.keyMetrics[]`: `total_visitors` (Plausible, business), `views_per_collection` (YT scrape, engagement), `top_5_gblocks_by_views` (YT scrape, engagement), `seo_rank_target_queries` (manual, growth), `yt_to_site_referral` (Plausible referrer, growth), `paying_members` (Stripe dormant, business). Shape matches existing portfolio entries (BFR reference). Null sections: `stripe`, `purgatory`, `sleeper`. Dynamic section: `readiness: blocked`, `stripeMode: off`, `freshness: active`. Cross-repo commit and push in lexcorp-war-room.
- Phase 4 / Step 4.5: Decommissioned Boston Founder Radio in lexcorp-war-room. Modified `/Users/georgele/projects/apps/lexcorp-war-room/portfolio/boston-founder-radio.yaml`: set `status: Archived`, `lifecycleState: archived`, added `archivedAt: "2026-04-27"` and `archivedReason` citing spec §2 (no real guest pipeline, content migrated to gBlockParty collections). File preserved in-place per war-room archive convention (not deleted). Cross-repo commit in lexcorp-war-room.
- Phase 4 / Step 4.4: Surfaced YouTube view counts on gBlock detail pages. Created `apps/web/src/lib/content/views.ts` with two exports: `loadViewCounts(contentRoot)` reads `data/youtube-views.json` and returns `Map<videoId, viewCount>` (empty map if file missing), `extractVideoId(url)` extracts YouTube video ID from watch/shorts/embed/youtu.be URL patterns. Modified `apps/web/src/app/[collection]/[slug]/page.tsx` — removed local `extractYouTubeId()`, imported shared `extractVideoId` and `loadViewCounts` from `@/lib/content`, added view count lookup in `GBlockDetailPage` and renders "▶ X views" span (`text-xs text-ink-soft`) in metadata bar after the date when a matching video ID has scraped data. Re-exported both functions from `apps/web/src/lib/content/index.ts`. Skipped GBlockCard changes (deferred — minimal value for list views). `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green, `pnpm -w test` 33/33 green.
- Phase 4 / Step 4.3: Created GitHub Action at `.github/workflows/scrape-youtube.yml` for nightly YouTube view-count scraping. Triggers: `schedule: cron "0 6 * * *"` (6 AM UTC daily) + `workflow_dispatch` for manual runs. Steps: checkout, setup Node 20, run `node scripts/scrape-youtube.mjs`, check if `data/youtube-views.json` changed via `git diff --quiet`, if changed commit with `chore(data): update youtube-views.json [skip ci]` and push, then trigger Vercel rebuild via `VERCEL_DEPLOY_HOOK` secret (deploy hook URL, must be configured in GitHub repo settings). Workflow has `contents: write` permission for the push. Vercel rebuild step guarded by `if: env.VERCEL_DEPLOY_HOOK != ''` so it no-ops gracefully when secret isn't set. `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (11 pages), `pnpm -w test` 33/33 green.
- Phase 4 / Step 4.2: Created YouTube view-count scraper at `scripts/scrape-youtube.mjs` — zero-dependency Node.js ESM script (`node:https`, `node:fs`) that fetches `https://www.youtube.com/@GeorgeLe/videos`, extracts `ytInitialData` JSON from page HTML via brace-depth parsing, walks `twoColumnBrowseResultsRenderer.tabs[Videos].richGridRenderer.contents[].richItemRenderer.content.videoRenderer` to extract `{ videoId, title, viewCount, publishedTimeText }` per video, writes `data/youtube-views.json` with `{ scrapedAt, videos }` shape. Exits non-zero with descriptive error if `ytInitialData` can't be parsed or zero videos extracted. Created `data/.gitkeep` for directory tracking. Local run produced 30 videos. Note: plan referenced `@GeorgeQLe` but actual channel handle is `@GeorgeLe` (verified via HTTP 200 vs 404). `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (11 pages), `pnpm -w test` 33/33 green.
- Rebrand cleanup: removed the stale `content/collections/boston-founder-radio.yaml` file so Boston Founder Radio no longer exists as a live gBlockParty collection surface. The current posture is now consistent across repo state and spec/docs: BFR is historical source material only, while `gcanbuild`, `weekly-sota`, and `weekly-g` remain the active collection set.
- Phase 4 / Step 4.1: Embedded Plausible analytics script in `apps/web/src/app/layout.tsx`. Imported `Script` from `next/script`, added `<Script src="https://plausible.io/js/script.js" data-domain={domain} strategy="afterInteractive" />` inside `<body>`, guarded by `process.env.NEXT_PUBLIC_PLAUSIBLE_DOMAIN` — script only renders when the env var is set (no tracking in dev/preview). `pnpm -w -r typecheck` clean (2 packages), `pnpm --filter @gblockparty/web build` green (12 pages), `pnpm -w test` 33/33 green (no regressions).
- Phase 4 planning: Decomposed Phase 4 (Polish) into 9 implementation steps using `tests-after` strategy. 4 independent tracks: Plausible analytics (Step 4.1), YouTube view-count scraper + GitHub Action + build-time view injection (Steps 4.2–4.4), lexcorp-war-room cross-repo edits (Steps 4.5–4.6), PaywallCard polish (Step 4.7). Green steps: regression tests (Step 4.8) + final verification (Step 4.9). Manual tasks: Plausible site creation, GitHub repo secrets for Vercel deploy hook. Archived Phase 3 to `tasks/phases/phase-3.md`. Updated `tasks/roadmap.md` with Phase 4 implementation steps and Phase 3 milestone checked off.
- **Phase 3 complete.** Step 3.10: Final verification gate — ran `pnpm -w test` (33/33 green: 14 schema + 5 loader + 14 pages), `pnpm -w -r typecheck` (clean, 2 packages), `pnpm --filter @gblockparty/web build` (12 pages generated). All three commands exit 0. Phase 3 milestone fully checked off. Phase 3 shipped 10 steps over 3 days: 4 canary MDX gBlocks (3 GCanBuild tutorials + 1 Weekly SOTA episode), production build config (metadataBase), GitHub repo, Vercel project + domain, live deployment verification, 5 regression tests, and final green-bar. Step 3.4 (Weekly G Ep 1) deferred — blocked on user recording; collection hidden via `HIDDEN_COLLECTIONS`. Next: Phase 4.
- Phase 3 / Step 3.9: Added 5 Phase 3 regression tests to `apps/web/src/__tests__/pages.test.ts` in a new `describe("Phase 3 regression — canary content")` block. Cases: (1) no placeholder video IDs (`dQw4w9WgXcQ`) in non-clip canary blocks, (2) all 3 GCanBuild canaries (excluding clips) have `type: tutorial`, (3) SOTA canary `sota-ep-001` has `type: episode` + `videoUrl` defined, (4) at least 2 canaries have `featured: true`, (5) `full-stack-web-app` slug exists in loaded blocks. `pnpm -w test` 33/33 green (14 schema + 5 loader + 14 pages), no regressions.
- Phase 3 / Step 3.8: Verified live production deployment at `https://gblockparty.com`. All smoke tests pass: TLS valid (HTTP/2 200, `strict-transport-security` header, Vercel-served), homepage renders Featured rail with pinned canaries (pastebin-clone, full-stack-web-app) and firehose feed. Collection pages all return 200: `/gcanbuild` (3 tutorials + 1 clip), `/weekly-sota` (1 episode), `/boston-founder-radio` (empty — no content yet, expected). Detail pages render type-appropriate headers: tutorials show hero images + YouTube `<iframe>` embeds, SOTA episode shows YouTube embed. `/g/<slug>` short-links 301 redirect to canonical `/<collection>/<slug>` URLs for all 4 content slugs (pastebin-clone-nextjs, better-auth-tutorial, full-stack-web-app, sota-ep-001). `/g/bfr-ep-001` correctly returns 404 (no BFR content exists). OG metadata verified on homepage (`og:title`, `og:description`) and detail pages (`og:title`, `og:description`, `og:image` with absolute `https://gblockparty.com` URLs). No issues found — no code changes needed. Verification-only step.
- Phase 3 / Step 3.7: Created Vercel project `gblockparty` via `vercel link --yes`. Configured project settings via Vercel API: root directory set to `apps/web`, framework set to `nextjs`. Domain `gblockparty.com` was already Vercel-registered (purchased 2024-10-25, nameservers at `ns1/ns2.vercel-dns.com`) but assigned to old `gblocks` project. Deleted old `gblocks` project (`vercel project rm gblocks`) to free the domain, then added both `gblockparty.com` and `www.gblockparty.com` to the new project. No DNS changes needed — domain is Vercel-managed with correct nameservers. Upgraded `next-mdx-remote` from 5.0.0 to 6.0.0 (`pnpm --filter @gblockparty/web add next-mdx-remote@^6.0.0`) because Vercel blocked deployment of v5 as vulnerable. v6 is API-compatible (same `next-mdx-remote/rsc` export with `compileMDX`). Production deploy succeeded via `vercel deploy --prod` — build green (12 pages), site live at `https://gblockparty.com`. GitHub repo integration (`vercel git connect`) failed — Vercel GitHub App not authorized for `GeorgeQLe/gBlockParty`; user must authorize in browser at Vercel dashboard Settings → Git. `pnpm -w test` 28/28 green, local build green (12 pages).
- Phase 3 / Step 3.6: Created GitHub repo and pushed `master`. Repo `GeorgeQLe/gBlockParty` already existed on GitHub (archived from a prior project). Unarchived via `gh repo unarchive`, added `origin` remote, pushed `master` branch with `git push -u origin master`. Repo is public, unarchived, accessible at `https://github.com/GeorgeQLe/gBlockParty`. Default branch on GitHub is `main` (stale from old project); all current work is on `master`. No code changes — CLI-only step.
- Phase 3 / Step 3.5: Set `metadataBase: new URL("https://gblockparty.com")` in `apps/web/src/app/layout.tsx` root metadata export — resolves OG image URL warning in production builds. Verified `next.config.ts` is minimal and Vercel-ready (no blockers). `pnpm -w test` 28/28 green, `pnpm --filter @gblockparty/web build` green (12 pages, no metadataBase warning).
- Phase 3 / Step 3.4 deferred: Weekly G Ep 1 canary blocked on user recording video. Hidden weekly-g collection from all site surfaces (homepage featured rail, firehose feed, collection page, detail pages, `/g/` short links) via `HIDDEN_COLLECTIONS` constant in `apps/web/src/lib/hidden-collections.ts`. Filtered in 5 files: `page.tsx` (homepage), `FirehoseFeed.tsx` (removed from COLLECTIONS array), `[collection]/page.tsx` (generateStaticParams), `[collection]/[slug]/page.tsx` (generateStaticParams), `g/[slug]/route.ts` (404 for hidden). Moved Step 3.4 to deferred follow-ups in `tasks/todo.md`. Build green (12 pages, down from 14). 28/28 tests still green (tests use raw loader, unaffected).
- Phase 3 / Step 3.3: Upgraded Weekly SOTA canary `content/gblocks/weekly-sota/sota-ep-001.mdx` from placeholder to production canary. Updated video ID from `9bZkp7q19f0` (Gangnam Style placeholder) to `_G8iJRMiCJY` (real video: "The AI Landscape as of Spring 2026 | Weekly SOTA Ep 1"). Updated frontmatter: title to "SOTA Ep. 1 — The AI Landscape as of Spring 2026", summary updated, tags changed to `[ai-landscape, state-of-ai, spring-2026, weekly-recap]`. MDX body rewritten as show-notes style: intro paragraph, big-picture framing, "what shipped" / "what stalled" / "what to watch" sections covering coding agents, context windows, open-weight models, multimodal, agent reliability, MCP standards, cost curves, and regulation. `<YouTube>` embed + `<Callout>` for pilot episode note. `featured: false` preserved. User chose reframe strategy (existing video as SOTA Ep 1). `pnpm -w test` 28/28 green, `pnpm --filter @gblockparty/web build` green (14 pages).

## 2026-04-25
- Phase 3 / Step 3.2: Added third GCanBuild canary — `content/gblocks/gcanbuild/full-stack-web-app.mdx`. Real YouTube video ID `5vdooX7g7AE`, title "Full Stack Web App Tutorial w/ Next.js, Better Auth, Neon DB, Drizzle ORM, tRPC, and Tanstack Query". MDX body: ~600-word 6-step walkthrough (scaffold Next.js, Neon + Drizzle, tRPC, Tanstack Query, Better Auth, end-to-end wiring) with `<YouTube>` embed, `<RepoCard>` linking `GeorgeQLe/walkthrough-starting-point-nextjs-trpc-betterauth-drizzle-neon-tanstack`, `<Callout>` tips, cross-link to pastebin tutorial. Frontmatter: `type: tutorial`, `collection: gcanbuild`, `featured: false`, tags `[nextjs, better-auth, neon, drizzle, trpc, tanstack-query, full-stack]`, heroImage placeholder. Updated test assertion from 5→6 fixture gBlocks in `pages.test.ts`. `pnpm -w test` 28/28 green, `pnpm --filter @gblockparty/web build` green (14 pages).
- Phase 3 / Step 3.1: Upgraded 2 GCanBuild fixture tutorials to production canary content. Replaced placeholder video IDs with real YouTube IDs from George's channel: `pastebin-clone-nextjs.mdx` updated from `dQw4w9WgXcQ` (rickroll) to `NzfKEbgvA0A`, `better-auth-tutorial.mdx` updated from `L8_98i_bMMA` to `R3r27ldM-TQ`. Expanded both MDX bodies from ~200-word step outlines to ~500–800-word production walkthroughs with section summaries synthesized from real video descriptions/chapters, `<Callout>` tips, and accurate stack descriptions. Updated frontmatter: pastebin title reflects full stack (tRPC, Tanstack Query, Better Auth, Shadcn now in tags), better-auth title updated to match video, tags expanded on both. `RepoCard` URLs updated to real GitHub repos. `publishedAt` dates kept as-is (user did not provide real publish dates). `pnpm -w test` 28/28 green, `pnpm --filter @gblockparty/web build` green (13 pages).
- **Phase 2 complete.** Step 2.11: Final verification gate — ran `pnpm -w test` (28/28 green: 14 schema + 5 loader + 9 pages), `pnpm -w -r typecheck` (clean, 2 packages), `pnpm --filter @gblockparty/web build` (13 pages generated). All three commands exit 0 with no issues. No refactoring needed — no dead imports, unused vars, or type warnings surfaced. Phase 2 milestone fully checked off: all 10 acceptance criteria met. Phase 2 shipped 11 steps over 2 days: 5 fixture gBlocks, 6 components (GBlockCard, TypeBadge, CollectionBadge, FeaturedRail, FirehoseFeed, ShortsRail), 5 route patterns (home, collection, detail, redirect, tag stub), PaywallCard scaffold, SEO metadata, build-time slug uniqueness, and 9 regression tests. Next: Phase 3 (Launch).
- Phase 2 / Step 2.10: Wrote 9 regression tests in `apps/web/src/__tests__/pages.test.ts` covering Phase 2 acceptance criteria. Tests run against real `content/` fixtures via `loadAllGBlocks({ contentRoot })`. Cases: (1) `loadAllGBlocks()` returns 5 fixtures across 3 collections, (2) includes expected types (tutorial, episode, clip), (3) featured filter returns 2 `featured: true` blocks sorted by `publishedAt` desc, (4) clip filter returns 1 clip-type block, (5) redirect lookup resolves every slug to canonical `/<collection>/<slug>`, (6) all 5 fixture slugs findable by name, (7–9) `extractPreview` from `PaywallCard` — strips HTML/JSX tags, truncates at 150 words with ellipsis, passes through short text without ellipsis. Exported `extractPreview` from `PaywallCard.tsx` to enable direct logic testing. Slug uniqueness regression already covered by Phase 1 loader tests (case 3). `pnpm -w test` 28/28 green (14 schema + 5 loader + 9 pages), `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (13 pages).
- Phase 2 / Step 2.9: Build-time slug uniqueness verification + SEO metadata polish. Verified slug uniqueness propagation: created a temporary duplicate-slug fixture (`content/gblocks/weekly-g/streaming-highlight-clip.mdx` duplicating `gcanbuild/streaming-highlight-clip.mdx`), ran `next build` — build failed with clear error `Duplicate slug "streaming-highlight-clip" in gblocks/weekly-g/streaming-highlight-clip.mdx (also in gblocks/gcanbuild/streaming-highlight-clip.mdx)` via `generateStaticParams()` → `loadAllGBlocks()`. Reverted fixture. Added Open Graph metadata to all 5 page routes: root `layout.tsx` (default `type: "website"`, `siteName: "gBlockParty"`), `page.tsx` (home OG title + description), `[collection]/page.tsx` (collection name + description), `[collection]/[slug]/page.tsx` (block title + summary + conditional `og:image` from `heroImage`), `t/[tag]/page.tsx` (added missing `description` + OG title/description). Built HTML confirmed `og:title`, `og:description`, `twitter:card`/`twitter:title`/`twitter:description` meta tags present. `pnpm -w test` 19/19 green, `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (13 pages).
- Phase 2 / Step 2.8: Created `PaywallCard` component (`apps/web/src/components/PaywallCard.tsx`) — server component that extracts first ~150 words of raw MDX body as plain-text preview (strips JSX/MDX tags via regex, truncates at word boundary with ellipsis), renders in a `bg-muted` card with brutal border + `shadow-brutal`, overlays a gradient fade (`bg-gradient-to-t from-muted`) leading into a CTA block with "Become a member to read this gBlock" heading and a disabled "Join gBlockParty" button styled with `bg-accent-lime` + brutal border. Modified `apps/web/src/app/[collection]/[slug]/page.tsx` to import `PaywallCard` and conditionally render it when `block.membership === "member"` — skips MDX compilation for member blocks (returns `{ content: null }` instead of calling `compileMDX`), renders `PaywallCard` in place of the `<article>` while preserving all other page content (back link, type-specific header, title, metadata bar). Verified with temporary `membership: member` on fixture `pastebin-clone-nextjs.mdx` — "Become a member" and "Join gBlockParty" present in built HTML; reverted fixture after verification. `pnpm -w test` 19/19 green, `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (13 pages, no change — no member gBlocks in production fixtures).
- Phase 2 / Step 2.7: Created `/g/<slug>` vanity redirect route handler (`apps/web/src/app/g/[slug]/route.ts`) and `/t/<tag>` stub page (`apps/web/src/app/t/[tag]/page.tsx`). Route handler: `GET` export loads all gBlocks via `loadAllGBlocks({ contentRoot })`, finds block by `params.slug` (awaited — Next.js 15 async params), returns `NextResponse.redirect(url, 301)` to `/<collection>/<slug>` if found, or `new NextResponse("Not found", { status: 404 })` if not. Uses `resolveContentRoot()` pattern from Steps 2.5/2.6. Tag stub: server component with `generateStaticParams()` returning empty array (dynamic on-demand), `generateMetadata()` setting title from tag name (e.g., "Tag: nextjs"), renders heading with `#{tag}`, "Tag pages coming soon" message, and back-to-home link. Both routes use Next.js 15 async `params` pattern. `pnpm -w test` 19/19 green, `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (13 pages: `/g/[slug]` dynamic, `/t/[tag]` SSG with empty params).
- Phase 2 / Step 2.6: Created gBlock detail page route (`apps/web/src/app/[collection]/[slug]/page.tsx`) with `generateStaticParams()` returning all `{ collection, slug }` pairs from `loadAllGBlocks()`, `generateMetadata()` deriving title and description from block frontmatter, and a server component rendering type-specific headers: `VideoHeader` (YouTube `<iframe>` embed extracted from `videoUrl` via regex for episodes/streams/clips), `TextHeader` (hero image + reading time badge for tutorials/essays), `CodeHeader` (linked card to `repoUrl`/`demoUrl` for repos/tools/demos). MDX body rendered via `next-mdx-remote` v5's `compileMDX({ source, components })` from `next-mdx-remote/rsc` — RSC-compatible, returns `{ content }` JSX. Custom components wired: `YouTube`, `Callout`, `RepoCard`, `AudioPlayer`. Back link to `/<collection>`. Metadata bar with `TypeBadge` + `CollectionBadge` + formatted date. Also upgraded MDX component stubs: `YouTube.tsx` now renders responsive `<iframe>` embed with 16:9 aspect ratio + brutal border; `Callout.tsx` renders `<aside>` with left-border + tone-specific colors (info=blue, warn=coral, note=ink/muted); `RepoCard.tsx` renders brutalist card with monospace link + description. `AudioPlayer.tsx` left as-is (native `<audio>` is fine). Uses `resolveContentRoot()` pattern from Step 2.5. `notFound()` from `next/navigation` for missing blocks. `pnpm -w test` 19/19 green, `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (13 pages: home + 4 collections + 5 gBlock details + not-found).
- Phase 2 / Step 2.5: Created collection page route (`apps/web/src/app/[collection]/page.tsx`) with `generateStaticParams()` deriving slugs from `content/collections/*.yaml`, `generateMetadata()` for per-collection SEO, and a server component rendering collection hero (name + description from YAML) + responsive `GBlockCard` grid filtered to `block.collection === slug`. Type filter chips scoped to the collection (only types that exist in the collection are shown), linked via `?type=` search params (Next.js 15 async `searchParams`). Blocks sorted by `publishedAt` desc. Graceful empty state when no gBlocks in collection. Also introduced `resolveContentRoot()` helper (checks `process.cwd()/content` first, falls back to `../../content`) to handle Turbo running `next build` from `apps/web/` where `process.cwd()` differs from monorepo root — applied to both collection page and home page. Used `Link` instead of `<a>` for filter chips (client-side navigation). `pnpm -w test` 19/19 green, `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (8 pages: home dynamic + 4 collection SSG + not-found static).
- Phase 2 / Step 2.4: Wired home page route (`apps/web/src/app/page.tsx`) to load real content. Server component calls `loadAllGBlocks({ contentRoot: path.join(process.cwd(), "content") })` and renders `FeaturedRail` → `ShortsRail` → `FirehoseFeed` in order. `searchParams` (Next.js 15 async `Promise`) wired to `FirehoseFeed`'s `activeType`/`activeCollection` props for URL-param-based filtering (`?type=tutorial`, `?collection=gcanbuild`). Graceful empty state when no gBlocks exist (friendly message instead of blank sections). Added `metadata` export for SEO (`title` + `description`). Page is now dynamic (`ƒ`) due to `searchParams` usage. `pnpm -w test` 19/19 green, `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (home page renders with fixture data).
- Phase 2 / Step 2.3: Created 3 home-page section components under `apps/web/src/components/`: `FeaturedRail.tsx` (filters `featured: true`, sorts by `publishedAt` desc, limits to 3, renders `GBlockCard`s in responsive grid, returns `null` when empty), `FirehoseFeed.tsx` (renders all blocks reverse-chron with `GBlockCard`s, link-based type and collection filter chips using `<a>` tags for server-component compatibility, active chip styled with inverted colors, graceful empty state), `ShortsRail.tsx` (filters to `type === "clip"`, horizontal scroll with `overflow-x-auto`, fixed-width cards, returns `null` when no clips). All server components, no `"use client"`. Props accept `GBlock[]` from `@gblockparty/gblock-schema`. `FirehoseFeed` also accepts optional `activeType`/`activeCollection` for URL-param-based filtering. `pnpm -w test` 19/19 green, `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (4 static pages).
- Phase 2 / Step 2.2: Created 3 shared UI primitives under `apps/web/src/components/`: `TypeBadge.tsx` (pill badge colored by type category — accent-coral for video types, accent-blue for code types, ink for text types), `CollectionBadge.tsx` (linked pill badge with neutral surface styling, navigates to `/<collection>`), `GBlockCard.tsx` (composes both badges with brutal border + hard-offset shadow, hero image or type-colored placeholder, linked title to `/<collection>/<slug>`, formatted date, CSS-only hover lift effect using translate + shadow-brutal-lg). All server components, no `"use client"`. Types imported from `@gblockparty/gblock-schema`. Tailwind utilities resolve against Step 1.5 design tokens. `pnpm -w test` 19/19 green, `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (4 static pages).
- Phase 2 / Step 2.1: Seeded 5 development fixture gBlocks under `content/gblocks/` across all 3 collections. Files: `gcanbuild/pastebin-clone-nextjs.mdx` (tutorial, featured), `gcanbuild/better-auth-tutorial.mdx` (tutorial), `weekly-sota/sota-ep-001.mdx` (episode), `weekly-g/weekly-g-ep-001.mdx` (episode, featured), `gcanbuild/streaming-highlight-clip.mdx` (clip, parentSlug refs pastebin tutorial). Coverage: 3 types (tutorial, episode, clip), all 3 collections, 2 featured, 1 clip for shorts rail. Each MDX body uses at least one component stub (`<YouTube>`, `<Callout>`, `<RepoCard>`). `loadAllGBlocks({ contentRoot: "content" })` returns 5 blocks, all validate against `gBlockSchema`. `pnpm -w test` 19/19 green (no regression), `pnpm -w -r typecheck` clean, `pnpm --filter @gblockparty/web build` green (4 static pages).
- Phase 2 planning: Decomposed Phase 2 (UI Surface) into 11 implementation steps using `tests-after` strategy. 3 parallel lanes: components (GBlockCard, TypeBadge, CollectionBadge, FeaturedRail, FirehoseFeed, ShortsRail), routes (home, collection, gBlock detail, redirect, tag stub), paywall (PaywallCard). Step 2.1 seeds 5 development fixture gBlocks across all 3 collections exercising 3 types (tutorial, episode, clip) with `featured` and shorts-rail coverage. Steps 2.2–2.3 build presentational components as a micro-serial preamble. Steps 2.4–2.7 build routes in parallel with Step 2.8 (PaywallCard). Step 2.9 handles build-time slug uniqueness verification + SEO metadata polish. Steps 2.10–2.11 are the Green steps (regression tests + full verification). No manual tasks. Updated `tasks/roadmap.md` (inserted execution profile + implementation steps into Phase 2 section) and `tasks/todo.md` (replaced Phase 1 completed queue with Phase 2 working document).

## 2026-04-24
- Phase 1 / Step 1.1: Vitest scaffolded as the monorepo test runner (`vitest.config.ts`, root + `@gblockparty/gblock-schema` scripts, `test` task wired into `turbo.json`). `pnpm -w test` exits 0 with no tests present.
- Phase 1 / Step 1.2: Added 14 failing Vitest cases at `packages/gblock-schema/src/__tests__/schema.test.ts` pinning the discriminated-union contract (per-type required fields, episode videoUrl/audioUrl OR, unknown type, shared optional fields, membership default). Also added a package-local `packages/gblock-schema/vitest.config.ts` so `pnpm --filter` picks up `src/**/*.test.ts` (root include pattern didn't match when CWD is the package). Tests import via relative path (`../index`) so failures trace to the current flat schema, not module resolution. `pnpm --filter @gblockparty/gblock-schema test` and `pnpm -w test` both exit non-zero (7 red / 7 green) — the intended red state for Step 1.4 to target.
- Phase 1 / Step 1.3: Added failing MDX loader tests at `apps/web/src/lib/content/__tests__/loader.test.ts` pinning the contract for `loadAllGBlocks({ contentRoot })` and `loadCollection(slug, { collectionsRoot })` (DI via options, no env coupling). Five cases: valid fixtures parse + include body, invalid-frontmatter error surfaces the offending file path (`broken-episode.mdx`), duplicate `shared-slug` across collections throws, valid `gcanbuild.yaml` parses, invalid YAML throws. Fixtures seeded under `__tests__/fixtures/{valid,invalid,dupe}/gblocks/<collection>/<slug>.mdx` and `__tests__/fixtures/collections/{gcanbuild,bad}.yaml` — each content root has its own `gblocks/` tree so Step 1.6's loader just walks `<contentRoot>/gblocks/**`. Added `apps/web/vitest.config.ts` (package-local `include`) and `test` / `test:watch` scripts in `apps/web/package.json`. `pnpm --filter @gblockparty/web test` fails on missing `../loader` module (the red signal); `pnpm -w test` fails with the 7 schema reds plus the loader suite unable to resolve imports. Step 1.2 schema reds still present and unchanged.
- Phase 1 / Step 1.5: Replaced the placeholder TODO in `apps/web/src/app/globals.css` with the playful-brutalist token set per spec §4, exposed via a Tailwind v4 `@theme { ... }` block so `bg-bg`, `text-ink`, `shadow-brutal`, `rounded-md`, `font-mono`, etc. resolve as utilities. Tokens: colors (`--color-bg` cream `#FFF8E7`, `--color-ink` `#0B0B0B`, `--color-ink-soft` `#2A2A2A`, `--color-paper`, `--color-muted`/`--color-surface` `#EDE4C9`, `--color-primary`/`--color-accent-lime` `#C3F53C`, `--color-accent`/`--color-accent-coral` `#FF6B5B`, `--color-accent-blue` `#4D7CFE`); border widths 2.5px/3px + `--border-ink`; hard-offset no-blur shadows (`--shadow-brutal` 4px 4px, `--shadow-brutal-sm` 2px, `--shadow-brutal-lg` 6px); boxy radii (0 / 6 / 12 / 20); Inter Variable (sans/display) + JetBrains Mono Variable (mono) font stacks with system fallbacks — no `@font-face` / `next/font` this step (fonts slide to Step 1.10 per plan); px scale 12–72; motion with `--ease-out`/`--ease-in` per spec plus a `--ease-brutal: steps(4, end)` option. Body defaults applied on `html`/`body`; `code`/`kbd`/`pre`/`samp` get the mono stack. `postcss.config.js` untouched — Tailwind v4's inline `@theme` worked against the existing `@tailwindcss/postcss` plugin. `pnpm --filter @gblockparty/web build` succeeds (4 static pages); `pnpm --filter @gblockparty/gblock-schema test` exits 0 (14/14, no regression); `pnpm -w test` still red only on the Step 1.3 loader suite (unchanged shape, Step 1.6 scope).
- Phase 1 / Step 1.6: Installed MDX tooling (`gray-matter`, `js-yaml`, `next-mdx-remote` in deps; `@types/js-yaml` in devDeps) and implemented the content loader at `apps/web/src/lib/content/{paths,loader,collections,index}.ts` — sync API matching the Step 1.3 test contract. `loadAllGBlocks({ contentRoot })` walks `<contentRoot>/gblocks/<collection>/<slug>.mdx`, `gray-matter`-parses each file, validates frontmatter with `gBlockSchema.safeParse(data)` (body kept separate — schema doesn't include a `body` field, so it's attached post-parse), throws `${relativePath}: ${zodMessage}` on validation failure (Step 1.3 asserts `/broken-episode\.mdx/`), and maintains a `Map<slug, relativePath>` to throw on cross-collection duplicates with the slug named in the message. `loadCollection(slug, { collectionsRoot })` reads `${slug}.yaml`, `js-yaml`-parses, and lets `collectionSchema.parse()` throw. MDX component stubs seeded at `apps/web/src/components/mdx/{YouTube,RepoCard,Callout,AudioPlayer}.tsx` — minimal props-only placeholders (Phase 1 milestone; not exercised by Step 1.3 tests). No `import "server-only"` — Vitest runs in Node and the plain `fs` usage is fine; adding the guard would have tripped the test env. `pnpm --filter @gblockparty/web test` exits 0 (5/5); `pnpm -w test` exits 0 for the first time in Phase 1 (19/19 total); `pnpm -w -r typecheck` clean; `pnpm --filter @gblockparty/web build` still green (4 static pages). No changes outside the file list.
- Phase 1 / Step 1.7: Seeded the three active-collection YAMLs under `content/collections/` per spec §2. Overwrote the existing `gcanbuild.yaml` (which carried a paraphrased description) with verbatim spec copy, and created `weekly-sota.yaml` + `weekly-g.yaml`. Each file carries `slug` + `name` + `description` (verbatim §2); `frontDoorUrl` omitted on all three (spec §2 doesn't name a destination — the field is optional). Descriptions are long-form and include spec-level context (e.g. the Mailchimp-Clone underperformance note on GCanBuild) because the plan called for verbatim copy. Verified via a one-off `node --input-type=module` probe from `apps/web/` that `js-yaml.load` + `collectionSchema.parse()` accepts all three (no throwaway test committed). Pre-existing `content/collections/boston-founder-radio.yaml` left alone — it's a scheduled Phase 4 decommission (spec §2 marks BFR dropped, and it's listed in todo.md's Follow-ups) but out of scope for Step 1.7. `pnpm -w test` 19/19 green; `pnpm -w -r typecheck` clean (turbo-cached); `pnpm --filter @gblockparty/web build` still succeeds (4 static pages). No git remote — push is a local no-op.
- Phase 1 / Step 1.8: Green-bar verification — cold-ran `pnpm turbo run test typecheck --force` (cache bypass, no incremental mask): `@gblockparty/gblock-schema:test` 14/14, `@gblockparty/web:test` 5/5 (19/19 workspace-wide), typecheck clean across both packages. Cold rebuild (`rm -rf apps/web/.next && pnpm --filter @gblockparty/web build`) green — 4 static pages, compiled in ~1.5s. Probed `loadCollection()` against the real `content/collections/` tree via an ephemeral vitest file under `apps/web/src/tmp-probe/probe.test.ts` (stdout-only, deleted after run — not committed): all three slugs parsed and returned `GCanBuild` / `Weekly SOTA` / `Weekly G`. Same probe exercised `loadAllGBlocks({ contentRoot: '/Users/georgele/projects/apps/gblockparty/content' })` → returned `[]` as expected against the empty `content/gblocks/` (loader's `fs.existsSync` short-circuit worked). Aside: tried `node --experimental-strip-types` first — hit `ERR_MODULE_NOT_FOUND` on `./paths` because the loader files use extensionless imports (TS bundler resolution), which node's native strip-types resolver doesn't handle; vitest's esbuild resolver handles it, so routed the probe through it. No production diff surfaced — loader + collections modules are clean. Diff confined to `tasks/{todo,history}.md`. Push is still a local no-op (no remote on `master`).
- Phase 1 / Step 1.9: Workspace typecheck + build formal gate — cold-ran `pnpm -w -r typecheck --force` (2 packages, turbo cache bypassed): `@gblockparty/gblock-schema:typecheck` + `@gblockparty/web:typecheck` both clean in ~880ms. Cold build (`turbo cache cleared + pnpm --filter @gblockparty/web build`): 4/4 static pages, compiled in ~680ms. Both gates exit 0. No production diff — this step exists as a dedicated sign-off boundary before the Step 1.10 refactor. Diff confined to `tasks/{todo,history}.md`.
- Phase 1 / Step 1.10: Final refactor pass — converted `baseFields` plain-object spread in `packages/gblock-schema/src/index.ts` to a proper `baseSchema = z.object({...})` with `.extend()` per variant, which is more idiomatic Zod and eliminates the raw field-object intermediary. Cleaned barrel exports at `apps/web/src/lib/content/index.ts` to re-export all public symbols: added `LoadAllGBlocksOptions` and `LoadCollectionOptions` type exports that were missing. No other cleanup surfaced — loader, collections, and paths modules were already clean. `pnpm -w test` 19/19 green; `pnpm -w -r typecheck` clean (2 packages); `pnpm --filter @gblockparty/web build` green (4 static pages). Phase 1 milestone fully checked off.
- Phase 1 / Step 1.4: Rewrote `packages/gblock-schema/src/index.ts` as `z.discriminatedUnion("type", […])` over 8 variant schemas built from a shared `baseFields` spread. `episode` video-or-audio requirement is enforced via union-level `.superRefine()` (not `.refine()` on the variant) because Zod 3.25's `discriminatedUnion` refuses `ZodEffects` members — the earlier plan's assumption that `.refine()` on a variant worked turned out to be false in this Zod line; `.superRefine()` at the union level preserves per-variant strictness and keeps the union shape intact. `gBlockTypeSchema` expanded to the full 8-type enum (`clip`, `stream` added). Shared optional surface fields (`heroImage`, `videoUrl`, `featured`, `seriesSlug`) now live on every variant via the base spread. Also fixed a pre-existing Step 1.2 typecheck bug in `packages/gblock-schema/src/__tests__/schema.test.ts` — factory helpers typed their `overrides` as `Record<string, unknown>`, which made destructures like `const { videoUrl, ...rest } = stream()` fail TS2339; switched to generic `<T extends Record<string, unknown>>` so spread preserves keys in the return type. `pnpm --filter @gblockparty/gblock-schema test` exits 0 (14/14); `pnpm --filter @gblockparty/gblock-schema typecheck` clean. `pnpm -w test` still non-zero — only the Step 1.3 loader suite is red (unresolved `../loader` / `../collections`); schema regressions: none. `pnpm -w -r typecheck` — schema package clean; `apps/web` still red on the same loader imports (carried over from Step 1.3, Step 1.6 scope).
